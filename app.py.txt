from PyQt6.QtWidgets import QApplication, QWidget, QLabel
import sys

app = QApplication(sys.argv)

window = QWidget()
window.setWindowTitle("Offline GPT Test")
window.resize(400, 200)

label = QLabel("Offline GPT is working!", parent=window)
label.move(100, 80)

window.show()
sys.exit(app.exec())
